name: test-v140-candidate

on:
  push:
    branches:
      - ai-skill-v1.4-candidate-20260923
    paths:
      - .github/workflows/test-v140-candidate.yml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ai-skill-v1.4-candidate-20260923
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: npm test

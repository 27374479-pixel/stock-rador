name: v0.3-ci

on:
  push:
    branches:
      - ai-skill-v0.3-20260922
    paths:
      - .github/workflows/v0.3-ci.yml

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ai-skill-v0.3-20260922
      - uses: actions/setup-node@v4
        with:
          node-version: '24'
      - run: npm test

name: reveal-and-evaluate-2025q1

on:
  push:
    branches:
      - ai-skill-v0.2-20260922
    paths:
      - .github/workflows/reveal-and-evaluate-2025q1.yml

permissions:
  contents: write

jobs:
  reveal:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ai-skill-v0.2-20260922
          fetch-depth: 0

      - uses: actions/setup-node@v4
        with:
          node-version: '24'

      - name: Run tests before reveal
        run: npm test

      - name: Fetch post-lock historical prices
        run: |
          node scripts/fetch-backtest-prices.cjs \
            backtests/runs/2025q1-hn-v0.2-001/lock.json \
            backtests/runs/2025q1-hn-v0.2-001/prices.json

      - name: Evaluate frozen candidates
        run: |
          node scripts/evaluate-skill-run.cjs \
            backtests/runs/2025q1-hn-v0.2-001/lock.json \
            backtests/runs/2025q1-hn-v0.2-001/prices.json \
            backtests/runs/2025q1-hn-v0.2-001/outcome.json

      - name: Commit revealed outcomes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add backtests/runs/2025q1-hn-v0.2-001/prices.json
          git add backtests/runs/2025q1-hn-v0.2-001/outcome.json
          git commit -m "backtest: reveal 2025Q1 frozen outcomes"
          git push origin HEAD:ai-skill-v0.2-20260922
